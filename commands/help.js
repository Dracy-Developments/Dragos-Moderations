const { MessageEmbed } = require(`discord.js`)

module.exports = {
	name: 'ping',
    description: 'Ping!',    
    async execute (client, message, args) {
        const embed = new MessageEmbed()
        .setTitle(`🏓 Help Menu`)
        
	},
};